name: Auto Register Kiro Accounts (Sequential)

on:
  workflow_dispatch:

jobs:
  register-sequence:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      # 串行执行，一次只运行一个 job
      max-parallel: 1
      matrix:
        # 固定 3 个串行 job
        job_id: [0, 1, 2]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            wget \
            curl \
            unzip \
            gnupg \
            ca-certificates \
            fonts-liberation \
            fonts-noto-cjk \
            jq

      - name: Install Python dependencies
        run: |
          pip install --no-cache-dir -r requirements.txt

      - name: Install Playwright browsers
        run: |
          playwright install chromium
          playwright install-deps chromium

      - name: Clean up any existing WARP containers
        run: |
          # 停止并删除可能存在的旧容器
          docker stop warproxy-${{ matrix.job_id }} || true
          docker rm warproxy-${{ matrix.job_id }} || true

      - name: Start fresh Cloudflare WARP Proxy
        run: |
          echo "🚀 Starting fresh Cloudflare WARP proxy for Job ${{ matrix.job_id }}..."
          
          # 拉取最新镜像
          docker pull ghcr.io/kingcc/warproxy:latest
          
          # 启动新的 WARP 代理容器
          docker run -d --name warproxy-${{ matrix.job_id }} -p 1080:1080 ghcr.io/kingcc/warproxy:latest

          echo "⏳ Waiting for WARP to initialize (35s)..."
          sleep 35

          # 验证代理是否工作
          echo "🔍 Testing proxy connection..."
          for i in {1..10}; do
            if curl --socks5 127.0.0.1:1080 https://cloudflare.com/cdn-cgi/trace --connect-timeout 30 --max-time 45 --silent 2>/dev/null; then
              echo "✅ WARP proxy is working (attempt $i)"
              break
            else
              echo "⚠️ Proxy not ready yet, waiting 5s more... (attempt $i)"
              sleep 5
            fi
            if [ $i -eq 10 ]; then
              echo "❌ WARP proxy failed to start after 10 attempts"
              exit 1
            fi
          done

          # 获取当前 WARP IP
          echo "🌐 Getting current WARP IP address..."
          IP_INFO=$(curl --socks5 127.0.0.1:1080 https://ipinfo.io/json --connect-timeout 30 2>/dev/null || echo "{}")
          echo "Current Proxy IP Info:"
          echo "$IP_INFO" | jq '. | {ip: .ip, city: .city, region: .region, country: .country, org: .org}' || echo "Could not parse IP info"

      - name: Install kiro-cli
        run: |
          echo "📦 Installing kiro-cli using official installer..."
          
          # 确保目录存在
          mkdir -p ~/.local/bin
          
          # 下载并安装
          curl -fsSL https://cli.kiro.dev/install | bash

          # 添加到 PATH
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          
          # 等待一下确保路径生效
          sleep 2
          
          # 验证安装
          if ~/.local/bin/kiro-cli --version 2>/dev/null; then
            echo "✅ kiro-cli installed successfully"
          else
            # 尝试直接运行
            echo "Trying alternative installation check..."
            export PATH="$HOME/.local/bin:$PATH"
            kiro-cli --version && echo "✅ kiro-cli verified" || echo "⚠️ kiro-cli may have issues"
          fi

      - name: Create config.json
        run: |
          cat > config.json << 'EOF'
          {
            "imap": {
              "host": "${{ secrets.IMAP_HOST }}",
              "port": ${{ secrets.IMAP_PORT }},
              "username": "${{ secrets.IMAP_USERNAME }}",
              "password": "${{ secrets.IMAP_PASSWORD }}",
              "use_ssl": false,
              "starttls": true,
              "verify_cert": false
            },
            "email_domain": "${{ secrets.EMAIL_DOMAIN }}",
            "backend_url": "${{ secrets.BACKEND_URL }}",
            "admin_password": "${{ secrets.ADMIN_PASSWORD }}",
            "proxy": {
              "enabled": true,
              "server": "socks5://127.0.0.1:1080",
              "username": "",
              "password": ""
            }
          }
          EOF
          
          echo "📁 Config.json created for Job ${{ matrix.job_id }}"

      - name: Run registration (Job ${{ matrix.job_id }})
        
        env:
          DISPLAY: ':99'
          PYTHONUNBUFFERED: '1'
          # 设置代理环境变量（大写）
          HTTP_PROXY: 'socks5://127.0.0.1:1080'
          HTTPS_PROXY: 'socks5://127.0.0.1:1080'
        run: |
          echo "========================================"
          echo "🚀 Starting registration Job ${{ matrix.job_id }}"
          echo "========================================"
          
          # 显示当前环境信息
          echo "🌐 Proxy settings:"
          echo "HTTP_PROXY: $HTTP_PROXY"
          echo "HTTPS_PROXY: $HTTPS_PROXY"
          
          # 验证代理连接
          echo "🔍 Verifying proxy connectivity..."
          curl --socks5 127.0.0.1:1080 https://httpbin.org/ip --connect-timeout 30 && echo "✅ Proxy verified" || echo "⚠️ Proxy verification failed"
          
          # 运行注册脚本
          echo "📝 Registering 4 accounts..."
          python -u register.py --headless --count 4
          
          echo "✅ Job ${{ matrix.job_id }} registration completed"

      - name: Clean up WARP container for this job
        if: always()
        run: |
          echo "🧹 Cleaning up WARP container for Job ${{ matrix.job_id }}..."
          docker stop warproxy-${{ matrix.job_id }} || true
          docker rm warproxy-${{ matrix.job_id }} || true
          echo "✅ WARP container cleaned up"

      - name: Upload registration logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: registration-logs-job-${{ matrix.job_id }}-${{ github.run_number }}
          path: |
            identity_account_*.json
            error_*.png
            *.log
          retention-days: 7

      - name: Job completion delay
        if: success()
        run: |
          echo "⏳ Waiting 10 seconds before next job..."
          sleep 10

  summary:
    needs: register-sequence
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Generate summary
        run: |
          echo "## 📊 Registration Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Job Details:" >> $GITHUB_STEP_SUMMARY
          echo "- **Total sequential jobs:** 3" >> $GITHUB_STEP_SUMMARY
          echo "- **Accounts per job:** 4" >> $GITHUB_STEP_SUMMARY
          echo "- **Total accounts:** 12" >> $GITHUB_STEP_SUMMARY
          echo "- **Run number:** ${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Triggered by:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Execution Status:" >> $GITHUB_STEP_SUMMARY
          echo "- **Workflow status:** ${{ needs.register-sequence.result || 'unknown' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📁 Check artifact logs for detailed registration results from each job." >> $GITHUB_STEP_SUMMARY
